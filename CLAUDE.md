# CLAUDE.md — A Pachas

@AGENTS.md

## Claude Code

- Load the project skill matching a review, mobile verification, or authorized
  release request from `.claude/skills/` instead of reconstructing that workflow
  from chat history.
- For pull-request reviews, read `REVIEW.md` and end with either actionable
  findings or the exact verdict `ready to merge`.
